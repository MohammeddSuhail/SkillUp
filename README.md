# SkillUp
Final Year Project
TextView t = (TextView) findViewById(R.id.vtitle);
        String wholeThing = "";
        for (int i = 0; i < vid.length; i++) {
            wholeThing += vid[i] +"\n";
        }
        t.setText(vid[2]);
